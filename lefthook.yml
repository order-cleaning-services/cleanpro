pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx}"
      root: "frontend/"
      run: npx eslint {staged_files}
    prettier:
      glob: "*.{js,jsx}"
      root: "frontend/"
      run: npx prettier -w {staged_files}
      stage_fixed: true
